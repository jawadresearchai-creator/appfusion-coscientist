# Pending intake requests

Only JSON files conforming to `schemas/v1/intake-request.schema.json` belong here. APK binaries never belong in Git. Store them in the private Drive intake folder or another approved object store and reference them by immutable locator plus SHA-256.

Creating a request records intent; it does not waive the rights gate or authorize dynamic/network-interception analysis.
